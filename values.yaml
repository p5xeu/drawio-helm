---

replicaCount: 3

image_repo: "jgraph/drawio"
version: "latest"

port: 8080

create_route: true
create_networkpolicy: true

resources:
  limits:
    cpu: "500m"
    memory: "1024Mi"
  requests:
    cpu: "250m"
    memory: "512Mi"
...
